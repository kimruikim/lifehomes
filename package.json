{
  "name": "template",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "server": "php -S localhost:8000",
    "sass": "sass sub/assets/scss:sub/assets/css --style expanded --watch",
    "dev": "run-p server sass",
    "pro": "sass sub/assets/scss/:sub/assets/css/ --style compressed"
  },
  "dependencies": {
    "jquery": "^3.6.0",
    "postcss": "^8.4.12",
    "postcss-cli": "^9.1.0",
    "sass": "^1.50.0"
  },
  "devDependencies": {
    "npm-run-all": "^4.1.5"
  }
}
